<p id="notice"><%= notice %></p>

<p>
  <strong>Date:</strong>
  <%= @grant.application_date %>
</p>

<p>
  <strong>Details:</strong>
  <%= @grant.details %>
</p>

<%= link_to 'Edit', edit_grant_path(@grant) %> |
<%= link_to 'Back', grants_path %>

<%= react_component('GrantControls',
                      grant: @grant,
                      payee: @grant_payee || {},
                      statuses: @grant_statuses) if current_user.admin? %>

<%= react_component('CommentsSection', grant: @grant, current_comments: @comments) %>
